apply plugin: 'java'
apply plugin: 'application'

group = 'g'
mainClassName = 'g.pkg.Main'

compileJava {
    options.release = 11 // 即javac --release，覆盖sourceCompatibility和targetCompatibility
}

repositories {
    mavenLocal()    //  已内置，但断网时还需要显式声明在第一行。
    mavenCentral();
}

dependencies {
}
